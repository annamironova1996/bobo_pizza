import React from 'react';

const NotFoundPage = () => {
    return (
        <section>
            <h2>Похоже, такая страница не найдена!</h2>
        </section>
    );
};

export default NotFoundPage;
